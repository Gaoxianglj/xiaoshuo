# 《学院魔女》新正文与大修闭环

> 适用：新章节、整章重写、会改变章级结构或人物关系推进的大修。小范围勘误与单段润色可走简化流程。

## 一、角色与写权限

| 角色 | 主要职责 | 文件权限 |
|---|---|---|
| 主 Agent | 建立验收契约、调度、裁决、晋升正式稿、更新追踪、提交与推送 | 项目内写入 |
| `story-logic-reviewer` | 细纲硬逻辑；正文对细纲与契约的兑现 | 只读 |
| `consistency-checker` | 写前载荷；写后连续性、知情边界与级联影响 | 只读 |
| `narrative-writer` | 生成候选稿；按问题编号定向返修 | 只写指定候选稿 |
| `cold-reader` | 隔离设定后的普通读者体验 | 只读，且受信息防火墙约束 |
| `literary-editor` | 宏观与微观文学审查 | 只读 |

审查 Agent 不得改稿；写作 Agent 不得改细纲、正式正文、追踪或 Git。候选稿通过全部硬门后，才由主 Agent 晋升。

## 二、先建立验收契约

每次运行先建立本章契约，至少包括：

- `MUST_KEEP`：不可删除或改义的事实、场景和情感节点；
- `MUST_CHANGE`：本轮必须解决的问题；
- `MUST_NOT`：不得提前揭示、不得新增或不得越过的边界；
- `TARGET_EFFECT`：读者应获得的情绪与章级承诺；
- `STYLE_ANCHORS`：一至两个固定锚点；
- `DOWNSTREAM_CONSTRAINTS`：下一章已依赖的事实；
- `ACCEPTANCE_TESTS`：可以逐项判定的通过条件。

契约来自作者当次意见、建议文档与既定事实，不擅自增加作者目标。运行状态分为：`AGENT_ACCEPTED`、`AUTHOR_ACCEPTED`、`STYLE_ANCHOR_ELIGIBLE`；三者不得混同。

## 三、执行循环

1. 主 Agent 按契约修订细纲，写明时间、视角、场景目标、阻力、转折、信息边界、物件与章尾接口。
2. `story-logic-reviewer` 以 `OUTLINE_AUDIT` 审查细纲；同时 `consistency-checker` 以 `PREWRITE` 生成写前载荷。若有 BLOCKER，主 Agent 修纲后重新审查。
3. 细纲通过后固定版本与哈希。`narrative-writer` 只向候选路径写 v1；后续只按未关闭的问题编号定向修改，不无故整章重写。
4. 对同一候选哈希并行执行：`DRAFT_FIDELITY`、`POSTWRITE` 与 `COLD_READ`。哈希不一致的报告作废。主 Agent 合并同源问题、处理审查冲突，并给出唯一返修单。
5. 硬事实与契约通过后，`literary-editor` 先宏观后微观审查。只有 `PROJECT_BLOCKER` 自动退回；`MASTER_NOTE` 是非阻断建议；`AUTHOR_TASTE` 留给作者。
6. 每次修改后重新计算哈希。受影响的审查门必须重跑；纯局部文句修改至少重跑正文符合度、文学审查与确定性检查。
7. 全部硬门通过后，主 Agent 晋升正式稿，更新章节快照，再把跨章变化增量写入连续性主表、角色状态、时间线、伏笔、上下文、章节索引和建议执行记录中真正受影响的部分。
8. 查看最终差异与工作区状态，只暂存本轮明确文件；在 `main` 上提交。推送前 `git fetch origin main` 并确认本地未与远端分叉，再推送 `origin/main`。

## 四、返修与停止条件

- 细纲最多 3 轮、正文硬门最多 3 轮、文学审查最多 2 轮。
- 同一根因连续两轮未关闭，或两个权威资料相互冲突，停止自动循环并标记 `AUTHOR_DECISION`。
- 审查意见冲突时按“作者当次意见 > 验收契约 > 锁定细纲/设定 > 现行正文 > 审美偏好”裁决；主 Agent记录采纳与驳回理由。
- 不以“零建议”为通过标准。通过意味着没有未关闭 BLOCKER，而不是所有 Agent 都想不到更漂亮的写法。

## 五、确定性检查

不交给文学审美判断的项目应由主 Agent 或脚本直接核对：固定题记、章名、目标字符范围、专名、违禁提前揭示、视角、人称、遗留占位符、章节路径、Git 差异和候选哈希。报告必须引用正在审查的版本。

## 六、运行档案

大修或新章在 `追踪/审查/第NNN章/` 留存：验收契约、细纲审查、写前载荷、正文三门审查、文学审查与最终验收。档案记录结论和证据，不保存每一轮完整候选稿；历史正文按项目既有备份规则处理。
