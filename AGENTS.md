# 《学院魔女》项目入口

项目根目录是唯一工作区。先读 `AI_RULES.md`，再按任务分流；已读资料未变化时不重复加载。

| 任务触发条件 | 读取与执行 |
|---|---|
| 创作、细纲、设定调整或改稿 | 依次读 `Bible.md`、`设定/文风.md`、`追踪/上下文.md`、`追踪/连续性/README.md`；选读一至两个文风锚点，再按场景查相关主表与最近快照 |
| 新正文、整章重写、章级大修，或改变时空、人物状态、物件、知情边界的局部改稿 | 在上述输入后执行 `正文单线程工作流.md` |
| 仅错字、标点、措辞润色，且不改变事实或动作含义 | 读目标段、必要上下文与文风标准；复核差异，发现连续性变化则转正文工作流 |
| 只读审查、评价或剧情查询 | 读章节索引、目标正文及相关标准；输出发现，不自动改正文或建立通过快照 |
| 配置、脚本、目录或 Git 维护 | 仅读相关配置、脚本和引用，不启动创作流程 |
| 生成、转换或修改 DOCX | 另读 `Word生成规范.md` |

## Skill 触发边界

- 明确点名的 skill 按用户要求使用；自然语言请求按实际产物选择，不因出现“小说”“审查”等词就加载整套工具箱。
- 长篇细纲、续写或正文改稿适用 `story-long-write`；配置维护和普通问答不触发。
- `story-review` 用于明确要求该审查流程；常规完稿自检直接执行项目工作流。
- DOCX、封面等任务只按实际交付物加载对应 skill。通用 skill 不扩大修改授权，不覆盖项目约束。

正文创作与审查全流程由当前主线程完成，禁止委派写作、研究、连续性、冷读或编辑子 Agent。`追踪/审查/` 是停用档案，不恢复其中旧流程。

文风以 `设定/文风.md` 和作者意见为准：002—008为正向样本，001仅供剧情设定参考，009仅作偏移校准。新增锚点须作者明确认可；前一章只负责剧情衔接。
