# 《学院魔女》项目指引

开始任何创作或改稿任务前，依次阅读：`AI_RULES.md`、`Bible.md`、`设定/文风.md`、`追踪/上下文.md`、`追踪/连续性/README.md`；再按本次场景从 `设定/文风锚点.md` 选读一至两个锚点，并从连续性主表与最近章节快照提取本章载荷。前一章用于衔接剧情，不得取代固定文风基准。

项目根目录是唯一工作区。`原始资料/` 只读，`正文/` 不在未授权时改写，`novel-project/` 是历史重复副本。任何新增剧情都应先建立细纲，再同步更新 `追踪/`。

本项目适合使用 `story-long-write` 流程：续写前检查角色状态、时间线、伏笔与待确认事项；发生设定冲突时记录并向作者确认，不擅自裁决。

次要人物、地点陈设、关键物件与知情边界统一由 `追踪/连续性/` 管理。匿名人物不得因职责相似自动合并；未写明的物件去向不得由写作 Agent 自行补全。每章正文完成后建立对应章节快照，再把会跨章复用的变化增量写入主表。

文风样本边界固定如下：002—008是正向样本；001是未经优化的初稿，只保留剧情与设定参考效力；009暂作偏移校准，不进入正向样本。新章节只有在作者明确认可文风后，才能加入 `设定/文风锚点.md`，禁止自动沿用可能已经偏移的上一章声线。
