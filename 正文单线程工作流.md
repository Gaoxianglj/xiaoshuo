# 正文单线程工作流

## 1. 触发与范围

- 新章、整章重写、章级大修：执行全流程。
- 局部修改改变时空、人物状态、物件位置或知情边界：检查修改场景、相邻接口及所有受影响的状态链；改变跨章复用事实时执行全章逻辑校验。
- 仅错字、标点、措辞调整且不改变含义：核对差异与上下文即可，不新建整套验收材料；若已有校验，更新哈希与本次复核范围。
- 只读审查：仅执行请求的检查，报告问题，不自动改稿、生成通过快照或更新事实主表。

所有步骤由当前主线程完成，禁止子 Agent。授权与停止边界见 `AI_RULES.md`。

## 2. 写前输入与细纲

按 `AGENTS.md` 的顺序读基础资料，再查章节索引确认当前版本。读取本章细纲、前章衔接段、后章锁定接口，以及相关角色状态、时间线、伏笔、待确认事项和专题设定。

先建立细纲，再从连续性三张主表与最近相关快照按 `追踪/连续性/模板_连续性载荷.md` 提取载荷。未定身份、未明物件状态保持标注，不擅自补定。

新章、整章重写和章级大修须在细纲或现有验收文件中列明以下条件；不重复建表：

- 必须保留、必须改变、禁止新增或提前揭示的内容；
- 目标情绪与表达幅度、可选文风参照、下游已依赖事实；
- 作者指定的标题、题记、镜头位置、视角与禁用项；篇幅按`设定/文风.md`区分建议预算和作者明确要求的硬指标，未明确要求时不自行设置数值门槛；
- 按 `大纲/模板_章节细纲.md` 独立记录人物心理／即时情绪载荷，并逐场标出心理转折。

审视细纲中的因果、时间移动、行动能力与权限、物件来源与状态、知情基础、情绪外显和上下章接口。发现问题先修细纲；作者裁决事项写入 `追踪/待确认事项.md`。

## 3. 写作与基础检查

修改正文前备份至 `正文/BK/`，名称注明日期与修改前状态。按授权范围和细纲写作，依人物与剧情选择对白、行动、心理、景物及修辞的幅度；大多数平常时刻保持克制含蓄，关键情感处局部展开，避免把整章写成持续高潮。

落盘后核对标题、题记、路径、专名、人称视角、作者指定指标，以及占位符、异常重复、截断和工程元信息。标点检查保留有意停顿。修复后重跑受影响检查。

字数统计用于了解篇幅，不因低于细纲预算就补写；通用 skill 的补足比例和最低字数不覆盖本项目文风规则。`chapter_gate.py` 的 `--min-chars`／`--max-chars` 仅在作者明确要求硬性篇幅时传入。内部禁止事项用来检查已写事件，不要求每项在正文出现一句“没有发生”。

## 4. 完稿逻辑校验

重新阅读实际落盘正文，按 `追踪/连续性/模板_完稿逻辑校验.md` 写入 `追踪/连续性/完稿校验/第XXX章.md`，记录正文哈希与检查范围。

先提取逐场景状态账本：时间、地点与出入口、在场人物及进出、物件位置与状态、门锁／容器、新增知情与结果。再逐段核对动作前提：

- 人是否在场，距离、门锁、视线与权限是否允许行动；
- 物件是否可触及，持有人、开合、损坏、消耗状态是否匹配；
- 双手占用、姿势、负重、伤势和魔法负荷是否允许；
- 必要的靠近、放下、打开、解锁或耗时是否有依据，动作结果是否被后文继承。

随后按时间、空间、物件、人物四个维度定向回读，并核对前章终态、后章接口、相关主表与快照。校验记录不得替正文补动作。

### 分级与处理（连续性唯一口径）

| 等级 | 触发条件 | 处理 |
|---|---|---|
| S1 | 正文明示事实或时空硬矛盾，如同一人同时在两地 | 修正文并复验，阻止验收 |
| S2 | 因果、权限、能力、知情或行为前提明确不成立 | 修正文并复验，阻止验收 |
| S3 | 已建立的物件、服装、伤势、门锁或陈设状态无依据跳变 | 修正文并复验，阻止验收 |
| S4 | 状态未写明、身份未定、可选照应或未来风险，尚无矛盾证据 | 标注与记录，不单独阻止验收，不自行补定 |

每项问题给出正文位置、前后状态和影响。仅未写出普通过渡不自动判错；只有行动前提不成立或既有状态冲突才升为 S1—S3。文风偏好在文学回读记录，不混入连续性等级。

修复从受影响场景的前一个状态节点重新推演，再跑全章相关定向回读。通过须满足：无未关闭 S1—S3；进出、门锁、换手、时间跳转有可指认依据；S4 保留标注；校验对应当前正文哈希。

正文改变后，旧哈希结论不能直接沿用：检查差异、重验受影响链并更新哈希；无关且未变部分可保留结论，注明复核范围。

## 5. 文学回读

按文风主档检查情绪是否充分、人物声线、空间清晰度、重复解释及跨场景表达机制。先核对多数平常时刻是否保持克制底色，再核对关键情感是否充分；锚点可选，连贯修辞不因密度判错，静物章尾不自动加分，爆发不以克制程度验收。不得把前一章自动升级为声线基准。

连读相邻对白，检查停顿是否被解释其含义的旁白填满，以及前章已说明的信息是否被重新演说。记录确有依据的修改和必要的保留例；没有问题则保留原文。脚本检查与文学判断分别记录，不以全部脚本通过推定文风合格。

文学修改若改变动作、站位、物件、时序或知情，返回第4节复验；纯措辞调整核对含义与差异后更新校验哈希。

## 6. 交付与同步

全部适用检查通过后，依次更新章节快照、跨章主表变化、相关角色状态／时间线／伏笔／上下文，再更新章节索引的哈希、字数与修改说明。无变化项不重复写入；不得把“本轮检查通过”写成作者终审定稿。

需要 Word 时另执行 `Word生成规范.md`。按 `AI_RULES.md` 完成 Git 同步，交付时说明产物、检查结果与待确认事项。

同一逻辑根因连续两轮未关闭时，先重新核对来源、定位根因并调整修法；仅在缺少作者决定、修改会越界或依赖外部条件时暂停相关部分。不得仅因轮数到限就中断可自行完成的修复。
