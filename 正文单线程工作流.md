# 《学院魔女》正文单线程工作流

> 适用：新章节、整章重写、章级大修，以及会改变时间、空间、人物状态、物件位置或知情边界的局部修改。

## 一、唯一执行原则

- 全流程由当前主线程连续完成，不调用、生成或委派任何写作、审查、冷读、文学编辑、研究或连续性子 Agent。
- 即使通用 skill 建议使用 `narrative-writer`、`consistency-checker`、`story-review` 或其他并行角色，本项目也一律由主线程直接执行。
- 细纲、正文、校验与追踪使用同一批已读取的项目资料，避免不同执行者只获得局部上下文。
- `追踪/审查/`中的旧文件是历史试验档案，不是当前流程模板，不得据此恢复多 Agent 闭环。

## 二、写前固定输入

开始任何正文工作前，主线程依次完成：

1. 阅读 `AI_RULES.md`、`Bible.md`、`设定/文风.md`、`追踪/上下文.md`、`追踪/连续性/README.md`。
2. 从 `设定/文风锚点.md`选择一至两个与场景匹配的正向锚点。
3. 从 `追踪/章节索引.md`确认当前正文、细纲和章节快照，禁止误用备份或历史副本。
4. 阅读本章细纲、前一章衔接段、后一章已经锁定的接口，以及本章相关角色、势力、世界观资料。
5. 从连续性主表和最近章节快照提取写前载荷，按`追踪/连续性/模板_连续性载荷.md`记录“不知道就会写错”的事实。
6. 遇到资料冲突时写入`追踪/待确认事项.md`并停在冲突边界，不自行裁决。

前一章只负责剧情衔接，不能取代固定文风锚点；`原始资料/`始终只读。

## 三、验收契约与细纲

新章、整章重写和章级大修先由主线程建立可判定的验收契约，至少写清：

- `MUST_KEEP`：必须保留的事实、场景、人物状态与情感节点；
- `MUST_CHANGE`：本轮必须解决的问题；
- `MUST_NOT`：不得新增、提前揭示或越过的边界；
- `TARGET_EFFECT`：目标情绪与读者体验；
- `CHARACTER_EMOTION_LOAD`：主要人物在各场景的即时情绪、触发原因、外显方式与禁止越界；
- `STYLE_ANCHORS`：本轮选定的一至两个锚点；
- `DOWNSTREAM_CONSTRAINTS`：后续章节已经依赖的事实；
- `ACCEPTANCE_TESTS`：标题、题记、字数、位置比例、视角、禁用内容等可直接判断的条件。

随后由同一主线程逐项审视细纲：

1. 事件是否都有原因、行动、结果与后果；
2. 场景之间的时间和移动是否成立；
3. 人物是否有能力、动机、权限和知情基础完成动作；
4. 物件是否有明确来源、位置、持有人与状态；
5. 本章终态是否能接上下章；
6. 目标情绪是否由场景交付，而不是靠解释总结补写。
7. 主要人物在每个关键情节中的即时情绪是否与当时所知、身体状态和关系阶段一致；情绪必须落到动作、对白、注意力或物件上，并标出不得提前出现的情感越界。

发现问题先修细纲，再进入正文。未确定的身份或物件状态保留`【身份未定】`、`【状态未写明】`，不得为了闭合逻辑而擅自补剧情。

## 四、备份与单线程写作

1. 修改现有正文前，先把当前正式稿复制到`正文/BK/`，文件名说明修改前状态与日期。
2. 局部修改只改授权段落及其必要回声；整章重写按已锁定细纲重新写。
3. 同一主线程在写作期间持续保有写前载荷，不把正文拆给不同执行者。
4. 以动作、物件、景物、神态和对白承载情绪；写完按`设定/文风.md`回读，删除已经被画面表达过的解释。
5. 修改若改变下一章依赖的事实，必须同步修正接口或向作者报告，不能只让当前段落表面通顺。

## 五、正文完成后的确定性检查

正文落盘后先执行可以机械判断的项目：

- 标题、题记、章节路径、视角与人称；
- 非空白字符数、关键镜头位置比例及作者指定禁止项；
- 正文工程元信息、占位符、异常重复、截断与阻断性 AI 句式；
- 专名、角色称呼、章节序号和 Git 差异范围；
- 标点规范脚本只处理确属无功能的异常，不能机械改掉有意停顿。

任何阻断项修复后都要重跑对应检查。

## 六、完稿逻辑与时空校验（硬门）

确定性检查后，主线程必须重新从头阅读实际落盘正文，不能依赖写作时的记忆。按`追踪/连续性/模板_完稿逻辑校验.md`建立或更新`追踪/连续性/完稿校验/第XXX章.md`。

### 6.1 逐场景状态账本

先把正文拆成场景或动作单元，每个单元至少记录：

| 单元 | 时间 | 地点与出入口 | 在场人物 | 人物进入／离开 | 关键物件位置与状态 | 门、锁、容器状态 | 新增知情 | 结果 |
|---|---|---|---|---|---|---|---|---|

不得只抄剧情摘要。账本必须能回答“谁在何处、能否碰到什么、做完后状态变成什么”。

### 6.2 相邻动作前提检查

逐段检查每个实体动作的前提：

1. 行动者此刻是否确实在场；
2. 行动者是否已经进入该空间，门槛、门、锁或距离是否允许；
3. 物件是否在可触及位置，是否被锁、封存、包裹或由他人持有；
4. 人物的双手、姿势、伤势、负重与魔法负荷是否允许下一动作；
5. 动作是否需要先放下、打开、解锁、靠近、获得许可或经过时间；
6. 动作结果是否被后文继承，还是下一句无说明恢复旧状态。

重点查找这类矛盾：

- 前一句人物仍在门外，后一句却拿起屋内钥匙；
- 门已关上或落锁，人物未开门便进出；
- 双手正抱箱子，下一动作却无过渡地同时使用双手处理别的物件；
- 物件已收入箱底、交给别人或损坏，后文无动作便重新出现在手边或恢复；
- 人物已经离场，后续对白或观察仍把他算作在场；
- 时间从清晨跳到黄昏、旅程跨越远距离，却没有过渡或所需时长；
- 角色使用了自己尚未获得的信息，或看见被墙、门、视角和距离遮挡的事物。

### 6.3 全章与跨章检查

状态账本完成后，再做四遍定向回读：

1. **时间遍**：时刻、昼夜、季节、事件先后、停留与路程耗时；
2. **空间遍**：人物站位、进入和离开、门窗楼梯、视线遮挡、移动路线；
3. **物件遍**：来源、持有人、位置、开合／损坏／消耗状态、权限与可触及性；
4. **人物遍**：伤势、服装、负重、能力代价、知情边界、称呼与关系状态。

随后对照前一章终态、后一章已写开头或细纲接口、连续性主表与最近快照。正文没有交代的变化不得由校验记录替正文补完。

### 6.4 问题分级、修复与通过条件

- `S1`：明确事实或时空硬矛盾；
- `S2`：因果、权限、能力、知情或人物行为前提明显不成立；
- `S3`：物件、服装、伤势、门锁、陈设等状态无说明跳变；
- `S4`：状态未写明、可选照应或未来风险，不一定构成错误。

发现`S1—S3`后必须先修改正文，再从受影响场景前一个状态节点重新推演，并重跑全章相关定向回读。不得用校验报告里的解释替代正文缺失的动作。

通过条件：

- 没有未关闭的`S1—S3`；
- 所有“人物进入／离开、门锁变化、物件换手、时间跳转”都能在正文找到可指认依据；
- `S4`已明确记录为`【状态未写明】`或后续风险，没有被擅自补定；
- 校验记录写明检查的正文哈希，哈希变化后旧结论自动失效，必须重跑受影响部分。

小范围改稿也不能跳过本门：至少检查修改场景、前后相邻场景和全部被改变的物件／时间／知情链；若改动会跨章复用，则执行完整全章校验。

## 七、文风与情绪回归

逻辑硬门通过后，主线程再完成文学回读：

- 是否兑现目标情绪和细纲关键节点；
- 人物对白是否共享了不该共享的声线；
- 是否存在动作后的同义解释、成簇比喻、连续模板句和作者总结；
- 场景是否有足够动作、表情、景物与感官细节，同时保持空间清楚；
- 章尾是否留下自然的关系余韵、问题或下一步推动力。

文学回改若改变动作、站位、物件或时序，必须返回第六节重跑完稿逻辑与时空校验。

## 八、追踪、提交与停止条件

全部硬门通过后：

1. 建立或更新章节快照；
2. 将跨章变化增量同步到次要人物、地点与陈设、关键物件、角色状态、时间线、伏笔和上下文；
3. 更新章节索引中的正文哈希、字数与本轮修改说明；
4. 查看最终差异，只暂存本轮授权文件；
5. 确认位于`main`，获取`origin/main`并检查分叉后提交、推送；
6. 确认本地`main`与`origin/main`一致，保留用户原有未提交改动。

出现以下情况时停止并向作者确认：权威资料互相冲突；修复需要改变作者锁定情节；同一逻辑根因连续两轮仍无法关闭；必须补写正文未授权区域才能成立。
