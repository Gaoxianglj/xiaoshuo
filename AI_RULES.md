# 《学院魔女》AI 协作规则

> 适用范围：本项目内所有续写、改稿、大纲、设定与审查工作。若与其他资料冲突，以本文件的“信息优先级”处理。

## 1. 工作边界

- `原始资料/` 是作者原创素材与早期底稿：**只读，不移动、不覆盖、不润色回写**。
- 001—011的当前内容工作基准统一由`追踪/章节索引.md`指定。正文是否正式终审由作者单独确认，不能因文件存在、生成DOCX或本轮资料整理而自动判定定稿。
- `正文/BK/`、带“原稿／人工修订／逻辑回炉前”字样的文件及未被章节索引选中的同章版本只用于历史核对，不得覆盖当前内容基准。
- 未经作者明确授权，不改动`正文/`内容。发现新口径与现行正文冲突时，先同步设定、大纲与追踪，并在章节索引或修改建议中标出待改段落。
- `设定/`、`大纲/`、`追踪/` 是后续创作的工作区；新信息必须落到对应文件，而非只留在对话中。
- `novel-project/` 为历史重复副本，不作为创作依据；需要核对旧版本时才查阅。

## 2. 信息优先级与冲突处理

1. 作者在当前对话中明确确认的内容；
2. 已落盘并标明日期的作者确认专用设定、当前幕纲与细纲；
3. `追踪/章节索引.md`指定的当前正文（用于确认已经写出的事件）；
4. `Bible.md`与`追踪/`中已标为“已确认”的条目；
5. `原始资料/`中的作者原创设定与早期正文（只读，用于追溯；若作者后来明确修订，以新确认口径为准）；
6. 大型历史汇编与AI补完文档。

出现冲突时：不得悄悄“修正”正文；在 `追踪/待确认事项.md` 记录冲突、影响与建议，暂停涉及该冲突的新增剧情，等待作者决定。

上述顺序用于剧情与设定事实。文风另以作者当次明确意见和 `设定/文风.md` 为最高依据：002—008为正向样本；001虽可提供剧情事实，但不提供文风；009暂时只作偏移校准。

## 3. 续写前的固定检查

1. 读 `Bible.md`、`设定/文风.md`、`追踪/上下文.md`、`追踪/角色状态.md`、`追踪/伏笔.md`、`追踪/章节索引.md`与`追踪/连续性/README.md`。
2. 按本章场景从 `设定/文风锚点.md` 选择一至两个正向锚点；不得使用001或未经作者认可的009作为文风样本。
3. 读取前一章正文与本章细纲；若本章没有细纲，先建立细纲，不直接写正文。前一章只用于情节承接，不自动提供文风或全部连续性事实。
4. 从细纲提取本章会出现的次要人物、地点、陈设、关键物件、服装、伤势和知情事项，读取 `追踪/连续性/` 三张主表与最近相关章节快照，按模板形成写前连续性载荷。
5. 只读取本章相关的角色、势力和世界观资料，避免把未到场的信息提前泄露。匿名人物身份相似但无证据时标记 `【身份未定】`，不得自动合并；物件去向未写明时不得自行补全。
6. 写完后按 `设定/文风.md` 回改，并执行`正文单线程工作流.md`中的完稿逻辑与时空校验：逐场景核对时间、空间、人物进出、门锁、物件可触及性、动作前提与知情边界；无未关闭S1—S3后才能建立章节快照，将跨章变化增量同步到连续性主表，再更新角色状态、时间线、伏笔与上下文。

本项目所有正文创作与审查均由当前主线程完成，不调用任何子 Agent。通用写作 skill 中关于并行写作、审查或研究 Agent 的建议在本项目不适用。

## 4. 叙事与人物硬约束

- 主视角以**莉莉安的第三人称限知**为准；切换他人视角必须有明确的章节/分节功能（信息差、反讽或政治暗线）。
- 莉莉安不是“马上看透一切”的强硬型主角。她在第一幕后处于创伤、麻木和自责中；她的善意要有代价，恢复也必须缓慢、可见、可反复。
- 玛琳的背叛不能被简化为恶人翻脸：她起初受债务胁迫，后来确有真情与愧疚；莉莉安是否原谅她是后期核心试炼，不能抢答。
- 艾尔文、阿尔瓦、奥莉维娅的善意都受制度束缚；不得为了爽感把他们写成全知全能的救场者。
- 精灵的一般不干预原则与莉莉安特别协议以`设定/世界观/精灵与原初之地.md`为准。核心不是宿命式“不得改变命运”，而是不得在她未充分知情、未同意时制造不可逆结果、剥夺有效选择；紧急救命只能做到最低限度。
- 学院秘密司法、越狱隐瞒与秘密搜捕以`设定/世界观/学院内部司法与秘密监狱.md`为准。塞西莉亚不知越狱，学院较早发现后压制消息并秘密搜捕；现行第005章旧段落须在获得正文改稿授权后局部校正。

## 5. 文风与内容尺度

- `设定/文风.md` 是完整可执行标准，本节只列硬约束。
- 使用克制、具体、可感的叙述；句子偏短而有力，但不把同一镜头切成连续碎段。用动作、物件、景物、表情、感官与停顿承载情绪，避免连续解释人物心理。
- 动作、物件或对白已经传达情绪时，不再追加总结其含义；章尾不由作者说明主题、成长或关系变化。
- 比喻少而准确，不连续叠加“像、仿佛、似乎”，不用修辞替代事件推进与空间说明。
- 日常对白应有停顿、偏转与生活动作，不要求人物一次说全前提、边界、责任和退出机制。正式工作交接、调查、审讯与谈判才允许较强的规范性。
- 莉莉安当前可以因处境频繁核算制度、规则、费用与工作；卢卡斯不能因此共享同一套“制度说明”声线。他应温和、自然、略带不高明的幽默，以行动兑现承诺，除正式领务外避免连续宣读保障条款。
- 第一幕的伤害场景以感受、后果和权力结构为重点，避免猎奇化、细节化地渲染暴力。
- 不凭空新增现代概念、网络语、系统面板、超出既有技术层级的物件。
- 魔法必须遵守“想象 + 魔力储备 + 精神负荷 + 物理限制”的规则；任何强力效果都要交代来源、代价或既有能力基础。

## 6. 文件与命名规则

- 正文：`正文/第NNN章_章名.docx`（既有文件不强制改名）。
- 细纲：`大纲/细纲_第NNN章.md`；每章应有目标情绪、核心事件、人物状态变化、章尾推动力。
- 新角色/势力/规则：分别建入 `设定/角色/`、`设定/势力/`、`设定/世界观/`；未确认内容用 `【待确认】` 标注。
- 事实状态只在 `追踪/` 维护；`Bible.md` 是高密度入口，不重复堆砌所有设定全文。
- 可复用次要人物、地点陈设、关键物件和知情边界写入 `追踪/连续性/`；每章变化写入 `追踪/连续性/章节快照/第XXX章.md`。一次性路人只留在快照，不强建正式人物卡。

## 7. 默认交付方式

- 作者说“续写”：先给本章细纲与连续性检查；确认后再写正文，除非作者明确要求直接成稿。重写第二至第五章时，先建立或确认对应细纲，再将新稿另存为正式版本，不覆盖参考稿。
- 作者说“修改”：先给诊断与修改方案；不重写已完成正文，除非作者授权。
- 作者说“设定”：区分“已确认”“推定”“待确认”，不可把 AI 推断伪装成作者定稿。

## 8. Word 正文生成与核验固定流程

> DOCX 以 WPS 打开效果为第一优先级，LibreOffice 渲染为自动化视觉核验环境。字体与嵌入统一由 `scripts/docx_fangsong.py` 处理，不在各章生成器中自行拼装 OOXML 字体包。

### 8.1 唯一推荐生成路线

1. 正文先以 UTF-8 Markdown 完成并通过内容检查，再由 `python-docx` 生成 Word；不要直接向 `.docx` 写正文，也不要覆盖作者原稿。
2. 生成新章节时，以 `scripts/build_stockton_ch1_docx.py` 为已验证模板：复制或改造成对应章节生成器，只修改 `SOURCE`、`OUTPUT`、文档标题和必要的章节元数据，正文样式函数原则上保持不变。`doc.save(OUTPUT)` 后必须调用 `embed_fangsong_docx(OUTPUT)`。
3. Python 必须使用 Codex 工作区依赖提供的解释器。先调用工作区依赖加载工具取得当前路径，不要假定系统 Python 已安装 `python-docx`。当前验证环境的解释器形式为：`.../codex-primary-runtime/dependencies/python/bin/python3`。
4. Markdown 解析规则保持简单明确：`# ` 为章标题，`> ` 为题记，`## ` 为居中分节标题，其余非空行均作为正文段落。内部章号若不应在成品中出现，必须在 Markdown 和 Word 提取文本中各检查一次。

### 8.2 已验证版式参数

- 纸张：Letter；四边页边距均为 1 英寸。
- 全文统一字体：`仿宋`；WPS 对应字体别名为 `仿宋-简`。正文 11 磅，两端对齐，首行缩进 22 磅，1.333 倍行距，段后 8 磅，开启孤行控制。
- 章标题：居中、18 磅、粗体、段后 18 磅，并与下一段同页。
- 题记：居中、10.5 磅、灰色、左右缩进 0.55 英寸、段后 18 磅。
- 分节标题：居中、11.5 磅、粗体、段前 14 磅、段后 10 磅，并与下一段同页。
- 页脚：居中页码，形式为“— 页码 —”，9 磅灰色。
- 设置字体时不能只写 `run.font.name`；必须同时写入 OOXML 的 `w:ascii`、`w:hAnsi`、`w:eastAsia` 与 `w:cs` 字体字段，否则中文可能在 LibreOffice 中回退或缺字。

### 8.3 WPS 优先的仿宋嵌入

- 字体命名以根目录示例 `这是一份使用仿宋字体的文档.docx` 为准：四类运行字体字段均写 `仿宋`，字体表同时登记别名 `仿宋-简`。该示例只用于确认 WPS 命名，本身没有嵌入字体。
- 构建字体从本机 WPS 安装目录动态读取 `FangS-SC.ttf`；当前常用位置为 `/Applications/wpsoffice.app/Contents/Resources/office6/fonts/FangS-SC.ttf`。若 WPS 安装位置变化，用环境变量 `FANGSONG_FONT_PATH` 指定，不把路径散落到各章脚本。
- 该字体的 `OS/2.fsType` 已核验为 `0x0008`（允许可编辑嵌入）。嵌入前仍由工具复核许可；若字体版本更换且许可不允许嵌入，必须停止生成，不得绕过。
- `scripts/docx_fangsong.py` 负责：统一 `w:ascii`、`w:hAnsi`、`w:eastAsia`、`w:cs`；登记 `仿宋/仿宋-简`；生成 OOXML 混淆字体部件、关系和内容类型；启用 `embedTrueTypeFonts`；完成字体内容哈希校验。
- 不把 WPS 字体复制成项目内的独立 `.ttf` 资源。字体原始数据约 13 MB，写入压缩后的 DOCX 后，当前每份成品约 7.4—7.5 MB；这是跨环境稳定显示的预期代价。
- 成品首先满足 WPS；LibreOffice 用于逐页渲染排版检查。不要再通过修改 Codex 运行时字体目录来解决本项目字体问题，嵌入字体应随文档自带。

### 8.4 必做核验

1. 保存后先运行 `scripts/docx_fangsong.py 成品.docx --verify-only`，确认字体关系、字体键、许可、字体哈希以及全部运行字体字段通过。
2. 用当前 `documents` 工具包中的 `render_docx.py` 将成品渲染为逐页 PNG，并保留生成的 PDF 仅作检查；不要只相信 `python-docx` 成功保存。
3. 从第一页到最后一页逐页查看原始清晰度图片，检查：中文缺字、字体回退、截字、段落重叠、页边距、标题孤立、分节标题位置、页码以及末页是否异常。
4. 使用 `python-docx` 重新打开成品并提取全部段落文本，至少核对：首段标题、段落数量、正文字符数、节数量，以及不应出现的内部代号。迁移既有文档时，还要与迁移前备份逐字比较。
5. 同时检查 Markdown 与 `.docx` 文件大小非零；Word 成品不得只有空壳、图片预览或转换失败的临时文件。
6. 条件允许时在 WPS 实际打开抽检字体框与分页；WPS 界面检查不能替代结构校验和逐页渲染。
7. 只有“脚本生成成功 + 字体嵌入校验通过 + 文本结构审计通过 + 全部页面视觉检查通过”四项都完成，才可向作者交付 Word。

### 8.5 本次成功样例

- 源文件：`正文/正文MD/第一章_在那森林与湖之地_重写版.md`
- 生成器：`scripts/build_stockton_ch1_docx.py`
- 成品：`正文/第一章_在那森林与湖之地.docx`
- 字体基准：四类运行字体字段均为 `仿宋`，嵌入字体解混淆后 SHA-256 必须为 `dd844721a51792855b1f8786eb725f593436be32db4b21a94e1be73c799b70f7`（仅对应当前 WPS 字体版本；版本变化时重新核验许可与哈希）。
- 迁移后审计：271 个段落、1 个文档节、提取正文 9735 个字符；LibreOffice 使用文档内嵌字体渲染 16 页，逐页检查通过。

## 9. Git 提交与远端同步规则

- 本项目唯一允许提交和推送的工作分支是 `main`，远端目标固定为 `origin/main`。
- 每次任务只要造成任何项目文件变动，任务结束前必须检查全部变动，将变动提交到本地 `main`，并推送到 `origin/main`；不得只修改文件而不完成 Git 同步。
- 开始修改前与提交前均须确认当前分支为 `main`。若不在 `main`，先停止写入并切回 `main`，不得把本项目变动提交到当前的其他分支。
- 远端分支 `deepseek_harness_PC` 有独立专用用途：禁止向该分支提交、推送、合并、变基、强制更新或删除，也不得把它设为默认提交目标。
- 除非作者另行明确授权，其他非 `main` 分支同样只读，不承载本项目日常文件变动。
- 推送前先获取远端状态并确认 `main` 是否分叉；若远端出现未知提交或冲突，先向作者报告，不擅自覆盖。完成后须确认本地 `main` 与 `origin/main` 指向同一提交，且工作区无遗漏变动。
